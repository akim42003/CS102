def main():
    alphabet = ["a", "b", "c", "d", "e", "f"]
    letter = input("Type a letter between 'a' and 'f': ")
    if letter in alphabet:
        print("The letter", letter, "is at index", alphabet.index(letter), "in", alphabet)

main()